// && - AND Date
// || - OR Date

let a = true;
let b = false;

console.log(a && b); // false - both a and b need to be true for AND to return true
console.log(a || b); // true - at least one of a or b needs to be true for OR to return true        
console.log(!a); // false - NOT operator negates the value of a