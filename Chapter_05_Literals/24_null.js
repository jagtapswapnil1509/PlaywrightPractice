let no_of_audi_has_swapnil = null; // This variable is intentionally set to null, indicating that it currently has no value.

let pramod_has_two_wifes; //undefined, as it has been declared but not assigned a value.

let audi = "";
console.log(typeof audi);